<h1><%= "Cleanups for the state of: #{@state_name}" %></h1>


<div>
 <% @cleanups.each do |cleanup| %>
    <li><%= link_to cleanup.city, state_city_path(params[:id], cleanup.city) %></li>
    <% end %>
</div>

